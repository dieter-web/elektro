<%- include('aufgabe.ejs',{}) %>
<%- include('../../../../../../../layouts/partials/ergebnis.ejs') %>

<script type="module">

    import {drawkennzeichnung} from '/modules/drawkennzeichnung.mjs'
    import {tabulate} from '/modules/tabulate.mjs'
    import {drawboard} from '/modules/drawboard.mjs'
    import {populate} from '/modules/populate.mjs'

    // Visualisierung
    import {vErder} from '/components/Betriebsmittel/Uebertragungswege/Leitung/Blankeleitung/Erder/Erder.mjs'
    // import {vSchraubverbindung} from '/components/Verbindungstechnik/LoesbareVerbindung/SchraubVerbindung/SchraubVerbindung.mjs'

    //Kabel für Tabelle und Visualisierung
    const dataW1=await populate( "http://localhost:8000/json/example/Aufgabe15/W1.json" )

    // Klemme für Visualisierung
    // const dataX1=await populate( "http://localhost:8000/json/example/Aufgabe15/X1.json" )

    drawkennzeichnung( dataW1,"#kennzeichnung" )
    tabulate( dataW1,"#ergebnis" )


    // Rückkopplung
    const tab=d3.selectAll( 'tbody' ).selectAll( 'tr' )._groups[ 0 ][ 0 ].innerText.split( '\t' )
    Material.value=tab[ 0 ]
    d.value=tab[ 1 ]
    l.value=tab[ 2 ]
    δ2.value=tab[ 3 ]

    // Visualisierung
    const g=drawboard()

    let vE=new vErder( dataW1 )
    // let vS=new vSchraubverbindung( dataX1 )

    g.append( () => vE.fdraw( dataW1 ) )
    // g.append( () => vS.fdraw( dataX1 ) )

</script>
