<%- include('aufgabe.ejs',{}) %>
<%- include('../../../../../../../layouts/partials/ergebnis.ejs') %>

<script type="module">

    import {drawkennzeichnung} from '/modules/drawkennzeichnung.mjs'
    import {tabulate} from '/modules/tabulate.mjs'
    import {DrawBoard} from '/sysFrontend/DrawBoard.mjs'
    import {populate} from '/modules/populate.mjs'

    import {vErder} from '/components/Betriebsmittel/Uebertragungswege/Leitung/Blankeleitung/Erder/Erder.mjs'

    const data=await populate( "Aufgabe15/W1.json" )

    drawkennzeichnung( data,"#kennzeichnung" )
    tabulate( data,"#ergebnis" )


    // Rückkopplung
    const tab=d3.selectAll( 'tbody' ).selectAll( 'tr' )._groups[ 0 ][ 0 ].innerText.split( '\t' )
    Material.value=tab[ 0 ]
    d.value=tab[ 1 ]
    l.value=tab[ 2 ]
    δ2.value=tab[ 3 ]

    // Visualisierung
    const view=new DrawBoard( 'grafik',640,480,[ 0,0,500,500 ] );

    const point={x: 320,y: 240};

    let W1=new vErder( data,point )
    W1.createNode();
    view.addNode( W1.node )


</script>
