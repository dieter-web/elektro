<%- include('aufgabe.ejs',{}) %>
<%- include('../../../../../layouts/partials/ergebnis.ejs') %>

<script type="module">

    import {tabulate} from '/modules/tabulate.mjs'
    import {drawboard} from '/modules/drawboard.mjs'
    import {populate} from '/modules/populate.mjs'
    import {vW} from '/d3/lib/Betriebsmittel/vW.mjs'

    //tabelle ergebnis
    const data=await populate( "http://localhost:8000/json/example/beispiel14.json" )

    tabulate( data,"#ergebnis" )


    // Rückkopplung
    const sel=d3.select( 'table#ergebnis' ).selectAll( 'tr' ).selectAll( 'td' )._groups[ 1 ]

    Material.value=sel[ 0 ].innerHTML
    U.value=sel[ 1 ].innerHTML
    p.value=sel[ 2 ].innerHTML
    l.value=sel[ 3 ].innerHTML
    a.value=sel[ 4 ].innerHTML
    I.value=sel[ 5 ].innerHTML


    // svg grafik
    const g=canvas( '#grafik',800,300,"0 0 900 100" )

    let dataW1=[ {
        opacity: 1,
        height: 1,
        width: 800,
        text: "W1"
    } ]

    g.append( () => vW( dataW1,[ 10,50 ] ) )

</script>
