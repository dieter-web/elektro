<%- include('aufgabe.ejs',{}) %>
<%- include('../../../../../layouts/partials/ergebnis.ejs') %>

import {Tabelle2} from "/sysFrontend/Tabelle.mjs";

const data=await populate( "example/Beispiel11/data.json" );

R1.value = data[0].Eigenschaften.Wert;
R2.value = data[1].Eigenschaften.Wert;
R3.value = data[2].Eigenschaften.Wert;
R4.value = data[3].Eigenschaften.Wert;
U.value = data[0].Parameter.U;

const table=new Tabelle2( data,"#ergebnis" );
table.tabulate();


// Visualisierung
const view=new DrawBoard( 'grafik',640,480,[ 0,0,500,500 ] );
import {vWiderstand} from '/components/Betriebsmittel.mjs';

// Einfügepunkt
const points=[
{x: 0,y: 240},
{x: 200,y: 240},
{x: 400,y: 240},
{x: 600,y: 240}
];

points.forEach((p,i) => {
let vR = new vWiderstand(data[i],p);
vR.createNode();
view.addNode(vR.node)
})
</script>
