<%- include('aufgabe.ejs',{}) %>
<%- include('../../../../../layouts/partials/ergebnis.ejs') %>




import {Tabelle2} from "/sysFrontend/Tabelle.mjs";

const data=await populate( "example/Beispiel11/data.json" );

const table=new Tabelle2( data,"#ergebnis" );
table.tabulate();


// Rückkopplung
//const tab=d3.selectAll( "tbody" )._groups[ 0 ][ 0 ].innerText.split( '\t' );

// TODO: neu schreiben !
// R1.value=tab[ 0 ]
// R2.value=tab[ 1 ]
// R3.value=tab[ 2 ]
// R4.value=tab[ 3 ]
// U.value=tab[ 4 ]

// Visualisierung
import {vWiderstand} from "/components/Betriebsmittel.mjs";
const view=new DrawBoard( 'grafik',640,480,[ 0,0,500,500 ] );

// Einfügepunkt
const points=[
{x: 100,y: 240},
{x: 300,y: 240},
{x: 500,y: 240},
{x: 700,y: 240}
];

points.forEach((p) => {
let shape = new vWiderstand(data,p);
shape.createNode();
view.addNode(shape.node)
})
</script>
