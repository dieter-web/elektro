<%- include('aufgabe.ejs',{}) %>
<%- include('../../../../../../../layouts/partials/ergebnis.ejs') %>

const data=await populate( "example/Aufgabe13/data.json" )

Material.value = data[0].Eigenschaften.Material;
lm.value = data[1].Eigenschaften.lm;
N.value = data[0].Eigenschaften.N;
d.value = data[0].Eigenschaften.d;

drawkennzeichnung( data[0],"#kennzeichnung" )
drawkennzeichnung( data[1],"#kennzeichnung" )

const table=new Tabelle( data[0],"#ergebnis" );
table.tabulate();
const table1=new Tabelle( data[1],"#ergebnis" );
table1.tabulate();



// const sel=d3.selectAll( 'tbody' ).selectAll( 'tr' )._groups[ 0 ][ 0 ].innerText.split( '\t' )
// Material.value=sel[ 0 ]
// lm.value=sel[ 1 ]
// N.value=sel[ 2 ]
// d.value=sel[ 3 ]

// Visualisierung
import {vSpule} from '/components/Betriebsmittel.mjs'
import {vSpulenkoerper} from '/components/Betriebsmittel.mjs'

const view=new DrawBoard( 'grafik',640,480,[ 0,0,500,500 ] );
const point={x: 320,y: 240};

let g1=new vSpule( data[0],point )
let g2=new vSpulenkoerper( data[1],point )
g1.createNode();
g2.createNode();
view.addNode( g2.node )
view.addNode( g1.node )
</script>
