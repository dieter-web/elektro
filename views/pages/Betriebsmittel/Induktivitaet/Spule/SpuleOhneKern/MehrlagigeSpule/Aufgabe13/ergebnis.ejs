<%- include('aufgabe.ejs',{}) %>
<%- include('../../../../../../../layouts/partials/ergebnis.ejs') %>

<!-- <div class="container text-left">
    <div class="row ">
        <table class="table table-bordered" id="ergebnis1">SK1</table>
    </div>
    <div class="row ">
        <table class="table table-bordered" id="ergebnis2">W1</table>
    </div>
</div> -->

<script type="module">

    import {tabulate} from '/modules/tabulate.mjs'
    import {drawboard} from '/modules/drawboard.mjs'
    import {populate} from '/modules/populate.mjs'
    import {vL} from '/d3/lib/Betriebsmittel/vL.mjs'

    // tabelle ergebnis
    const data=await populate( "http://localhost:8000/json/example/aufgabe13.json" )

    let dataSK1=data.Parameter.SK1
    let dataW1=data.Parameter.W1

    tabulate( data,"#ergebnis" )

    // Rückkopplung
    // const sel=d3.select( 'tbody' ).selectAll( 'tr' )._groups[ 0 ];

    // R1.value=sel[ 0 ].innerText.split( '\t' )[ 0 ];
    // R2.value=sel[ 1 ].innerText.split( '\t' )[ 0 ];
    // R3.value=sel[ 2 ].innerText.split( '\t' )[ 0 ];
    // R4.value=sel[ 3 ].innerText.split( '\t' )[ 0 ];
    // U.value=sel[ 0 ].innerText.split( '\t' )[ 1 ];


    // Visualisierung
    const g=drawboard()

    vL( data,[ 'L1' ],[ 50,50 ] ).map( ( e ) =>
    {
        g.append( () => e )
    } )

</script>
