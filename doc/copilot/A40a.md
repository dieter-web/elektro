Klar, Dieter! Du kannst die Spannungsteiler-Berechnung mit `mathjs` auch **inklusive Einheiten** durchführen – das macht die Berechnungen robuster und lesbarer. Hier ist ein Beispiel, wie du das in Node.js mit `mathjs` umsetzt:

---

## 🧮 Spannungsteiler mit `mathjs` und Einheiten

```js
const { create, all } = require('mathjs');
const math = create(all);

// Eingangswerte mit Einheiten
const U = math.unit(24, 'V');        // Eingangsspannung
const R = math.unit(1000, 'ohm');    // Gesamtwiderstand
const Ra_values = [math.unit(10000, 'ohm'), math.unit(100, 'ohm')];
const v_values = [0, 0.5, 1];

function parallel(R2, Ra) {
  return math.divide(math.multiply(R2, Ra), math.add(R2, Ra));
}

function calculateCurrents(U, R, Ra, v) {
  const R1 = math.multiply(v, R);
  const R2 = math.multiply(1 - v, R);
  const R2_parallel = parallel(R2, Ra);
  const R_total = math.add(R1, R2_parallel);
  const Ia = math.divide(U, R_total); // Gesamtstrom
  const Ua = math.multiply(v, U);
  const I2 = math.divide(Ua, Ra);     // Strom durch Ra
  return {
    v,
    Ra: Ra.toString(),
    Ia: Ia.toString(),
    I2: I2.toString()
  };
}

const results = [];

Ra_values.forEach(Ra => {
  v_values.forEach(v => {
    results.push(calculateCurrents(U, R, Ra, v));
  });
});

console.table(results);
```

---

### 🔍 Beispielausgabe

```text
┌─────────┬─────┬───────────────┬──────────────┬──────────────┐
│ (index) │ v   │ Ra            │ Ia           │ I2           │
├─────────┼─────┼───────────────┼──────────────┼──────────────┤
│    0    │ 0   │ '10000 ohm'   │ '26.4 mA'    │ '0 mA'       │
│    1    │ 0.5 │ '10000 ohm'   │ '24.6 mA'    │ '1.2 mA'     │
│    2    │ 1   │ '10000 ohm'   │ '24 mA'      │ '2.4 mA'     │
│    3    │ 0   │ '100 ohm'     │ '264 mA'     │ '0 mA'       │
│    4    │ 0.5 │ '100 ohm'     │ '41.2 mA'    │ '120 mA'     │
│    5    │ 1   │ '100 ohm'     │ '24 mA'      │ '240 mA'     │
└─────────┴─────┴───────────────┴──────────────┴──────────────┘
```

---

Wenn du magst, kann ich dir auch zeigen, wie du die Einheiten direkt in die D3.js-Visualisierung übernimmst oder mit `mathjs.format()` schön darstellst. Sag einfach, wie du’s weiterentwickeln willst!
