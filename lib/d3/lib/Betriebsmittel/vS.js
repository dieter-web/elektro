function vS(data, pos) {
  // Layer des shape
  // HINT: sind nicht die Layer des svg !!
  const Symbol = d3.create("svg:g").attr("id", "symbol");
  const Name = d3.create("svg:g").attr("id", "name");
  const Value = d3.create("svg:g").attr("id", "value");

  // hier Einfügen

  return S.node();
}
export { vS };